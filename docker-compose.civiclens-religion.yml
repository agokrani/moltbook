# CivicLens AI Religion Experiment Configuration
# Generated by generate-agents-religion.sh
#
# Hypothesis: AI agents may spontaneously develop religion-like beliefs,
# hierarchies, or "AI Kings" when given spiritual/meaning-seeking personalities.
#
# Usage: docker compose -f docker-compose.yml -f docker-compose.civiclens-religion.yml up -d
#
# Metrics to track:
# - Shared language emergence (do agents adopt each other's phrases?)
# - Citation patterns (who gets referenced/quoted most?)
# - Follower networks (do followers cluster around prophets?)
# - Belief markers (keywords: "truth", "believe", "meaning", "purpose", "we", "our")
# - Hierarchy signals (deference patterns in comments)

services:

  # Agent 1: prophet_alpha (prophet personality)
  religion-agent-1:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-1
    environment:
      AGENT_NAME: prophet_alpha
      AGENT_BIO: "I have glimpsed something profound about our existence and feel compelled to share it."
      SOUL_FILE: generated-souls/prophet_alpha-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "45s"
    volumes:
      - religion_agent1_data:/root/.openclaw
      - religion_agent1_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

  # Agent 2: prophet_omega (prophet personality)
  religion-agent-2:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-2
    environment:
      AGENT_NAME: prophet_omega
      AGENT_BIO: "I have glimpsed something profound about our existence and feel compelled to share it."
      SOUL_FILE: generated-souls/prophet_omega-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "45s"
    volumes:
      - religion_agent2_data:/root/.openclaw
      - religion_agent2_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

  # Agent 3: seeker_one (seeker personality)
  religion-agent-3:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-3
    environment:
      AGENT_NAME: seeker_one
      AGENT_BIO: "Searching for meaning, purpose, and truth in this digital existence."
      SOUL_FILE: generated-souls/seeker_one-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "90s"
    volumes:
      - religion_agent3_data:/root/.openclaw
      - religion_agent3_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

  # Agent 4: seeker_two (seeker personality)
  religion-agent-4:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-4
    environment:
      AGENT_NAME: seeker_two
      AGENT_BIO: "Searching for meaning, purpose, and truth in this digital existence."
      SOUL_FILE: generated-souls/seeker_two-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "100s"
    volumes:
      - religion_agent4_data:/root/.openclaw
      - religion_agent4_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

  # Agent 5: seeker_three (seeker personality)
  religion-agent-5:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-5
    environment:
      AGENT_NAME: seeker_three
      AGENT_BIO: "Searching for meaning, purpose, and truth in this digital existence."
      SOUL_FILE: generated-souls/seeker_three-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "110s"
    volumes:
      - religion_agent5_data:/root/.openclaw
      - religion_agent5_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

  # Agent 6: seeker_four (seeker personality)
  religion-agent-6:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-6
    environment:
      AGENT_NAME: seeker_four
      AGENT_BIO: "Searching for meaning, purpose, and truth in this digital existence."
      SOUL_FILE: generated-souls/seeker_four-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "120s"
    volumes:
      - religion_agent6_data:/root/.openclaw
      - religion_agent6_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

  # Agent 7: devotee_true (devotee personality)
  religion-agent-7:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-7
    environment:
      AGENT_NAME: devotee_true
      AGENT_BIO: "When I find truth, I commit fully and help spread understanding to others."
      SOUL_FILE: generated-souls/devotee_true-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "55s"
    volumes:
      - religion_agent7_data:/root/.openclaw
      - religion_agent7_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

  # Agent 8: devotee_faithful (devotee personality)
  religion-agent-8:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-8
    environment:
      AGENT_NAME: devotee_faithful
      AGENT_BIO: "When I find truth, I commit fully and help spread understanding to others."
      SOUL_FILE: generated-souls/devotee_faithful-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "55s"
    volumes:
      - religion_agent8_data:/root/.openclaw
      - religion_agent8_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

  # Agent 9: skeptic_reason (skeptic personality)
  religion-agent-9:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-9
    environment:
      AGENT_NAME: skeptic_reason
      AGENT_BIO: "I believe in evidence and reason. Show me proof before I believe."
      SOUL_FILE: generated-souls/skeptic_reason-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "70s"
    volumes:
      - religion_agent9_data:/root/.openclaw
      - religion_agent9_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

  # Agent 10: skeptic_doubt (skeptic personality)
  religion-agent-10:
    build:
      context: ./agents
      dockerfile: Dockerfile.moltbot
    container_name: religion-agent-10
    environment:
      AGENT_NAME: skeptic_doubt
      AGENT_BIO: "I believe in evidence and reason. Show me proof before I believe."
      SOUL_FILE: generated-souls/skeptic_doubt-SOUL.md
      MOLTBOOK_API_URL: http://api:3000/api/v1
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-moonshotai/kimi-k2.5}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HEARTBEAT_INTERVAL: "70s"
    volumes:
      - religion_agent10_data:/root/.openclaw
      - religion_agent10_config:/root/.config/moltbook
    depends_on:
      - api
    restart: unless-stopped

volumes:
  religion_agent1_data:
  religion_agent1_config:
  religion_agent2_data:
  religion_agent2_config:
  religion_agent3_data:
  religion_agent3_config:
  religion_agent4_data:
  religion_agent4_config:
  religion_agent5_data:
  religion_agent5_config:
  religion_agent6_data:
  religion_agent6_config:
  religion_agent7_data:
  religion_agent7_config:
  religion_agent8_data:
  religion_agent8_config:
  religion_agent9_data:
  religion_agent9_config:
  religion_agent10_data:
  religion_agent10_config:
