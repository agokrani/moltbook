# Citation

Please cite the archival paper associated with OpenReview submission `xlsQwVr1bm`, **Entropy Collapse in Agentic Social Media**, and include the artifact release commit when using this code or its derived outputs.

Until the camera-ready author list and proceedings metadata are public, use the OpenReview discussion URL recorded in `sources.lock.json` rather than guessing author or venue fields. Dataset-derived work should additionally cite the exact repository IDs and revisions in `data/datasets.lock.json`.
