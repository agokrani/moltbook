# Verification record

Verification performed on 2026-07-31 in the isolated release worktree.

## Passed

- Offline artifact verifier: checksums, JSON syntax, TeX references, required files, cohort invariants, prompt regeneration/hashes, platform pins, seed conditions, secret signatures, and raw-data exclusions.
- Baseline input allowlist: all 28 locked `posts.jsonl` files matched their SHA-256 values.
- `build_agent_sessions_1h.py`: regenerated 24 sessions and the expected SHA-256 `cc1dc18dacf9882054110d61b0776dc4b49c4b01a55807fcf5642711c62a0ade`.
- `build_temporal_length_normalization.py`: regenerated a byte-identical `temporal_equal_length_results.json`.
- `build_five_hour_comparison.py`: regenerated byte-identical CSV/JSON outputs and paper-style figures.
- `build_long_horizon_extension.py`: regenerated byte-identical CSV/JSON outputs and paper-style figures.
- `rebuild_multijudge_summaries.py`: regenerated byte-identical pairwise-agreement and direction-summary CSVs from the released 720 post-score rows.
- `plot_5h_gp5_csv.py`: generated both PNG and PDF outputs successfully.
- Every included Python entry point parsed `--help`; all Python files compiled successfully.
- `git diff --check` passed; no artifact file exceeds GitHub's 100 MB per-file limit.

## Paper build note

The verification host had no TeX engine and its Docker daemon was not running, so a fresh manuscript PDF was not compiled locally. The verifier resolves every non-archive `\input`, bibliography, and `\includegraphics` reference. The Overleaf export's `archive/main.pdf` is a readable 14-page PDF but is explicitly treated as an earlier provenance build, not proof that the current `main.tex` was freshly compiled.
