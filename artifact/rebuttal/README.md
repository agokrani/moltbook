# Rebuttal evidence snapshot

`rebuttal_final.md`, `EVIDENCE_CHECK.md`, the selected figures, and the structured outputs in `../analysis/results/` come from the authoritative rebuttal repository snapshot `main@7aa442f`.

The later branch `agent/rebuttal-evidence-and-reviewer-responses@2d95dd8` diverged from that final results snapshot. This release therefore imports only its reproducible `build_agent_sessions_1h.py` and retains the results, metrics, and reviewer evidence from `7aa442f`.

The three older comment-based Reddit JSONs are intentionally omitted because the final evidence check marks them deprecated. The post-to-post output is `../analysis/results/matched_1h_reddit_posts.json`.
